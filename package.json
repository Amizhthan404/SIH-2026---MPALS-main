{
  "name": "mplads-ai-monitoring",
  "version": "1.0.0",
  "description": "Full-stack AI-powered anomaly detection and monitoring system for MPLADS fund utilization (MoSPI)",
  "private": true,
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix server run db:setup",
    "dev": "npm --prefix server run dev",
    "build": "npm --prefix server run build",
    "start": "npm --prefix server run start",
    "db:migrate": "npm --prefix server run db:migrate",
    "db:seed": "npm --prefix server run db:seed",
    "db:setup": "npm --prefix server run db:setup"
  },
  "keywords": [
    "mplads",
    "anomaly-detection",
    "mospi",
    "express",
    "typescript",
    "postgresql",
    "sqlite"
  ],
  "author": "MoSPI / SIH Team",
  "license": "ISC"
}
